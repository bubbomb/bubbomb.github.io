import pinochlePalMobile from './main';
import {
  AppRegistry
} from 'react-native';


AppRegistry.registerComponent('pinochlePalMobile', () => pinochlePalMobile);
AppRegistry.runApplication('pinochlePalMobile', { rootTag: document.getElementById('react-app') });
